# calc/calc/__init__.py — empty marker file so `calc/calc/` is an
# importable Python package. The entry point logic lives in
# `__main__.py`, which `python -m calc` executes (via the wrapper
# script emitted by the python-direct convention).
