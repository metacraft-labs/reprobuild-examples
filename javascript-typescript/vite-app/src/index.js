// Minimal Vite app entry. Vite's library-mode build bundles this file
// (plus any transitive imports) into ``dist/index.js``.

export function greet(name) {
  return `hello, ${name}`;
}

export default greet;
